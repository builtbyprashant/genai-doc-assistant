# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Test coverage artifacts
.coverage
coverage.xml

# Virtual environments
.venv/
venv/
genai-doc-assistant/

# Secrets — only .env.example is committed, never .env
.env
.env.*
!.env.example

# Uploaded documents / local data
data/

# Private design docs: only docs/NotForGH/ stays out of the public repo.
# Top-level docs (PROJECT_SCOPE, API_CONTRACT, REQUIREMENTS, UI_SPECIFICATION,
# IMPLEMENTATION_PLAN) are published. (P-1)
docs/NotForGH/

# OS / editor noise
.DS_Store
Thumbs.db
