# Local dev + CI: backend runtime plus the test toolchain.
# To also run the Streamlit UI locally, install requirements-frontend.txt as well.
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0                # coverage reporting
httpx>=0.27                    # FastAPI TestClient
